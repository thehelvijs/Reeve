# Override for building the server from a source checkout instead of pulling the
# published image:
#   docker compose -f deploy/docker-compose.yml -f deploy/docker-compose.build.yml up -d --build
services:
  server:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.server
    image: reeve:source
    pull_policy: build
